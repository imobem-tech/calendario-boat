# ============================================================
# nixpacks.toml — V.2609140032
# Configuração do Railway para instalar PostgreSQL tools
# ============================================================

[phases.setup]
nixPkgs = ["nodejs_20", "postgresql"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["echo 'Build concluído'"]

[start]
cmd = "npm start"
